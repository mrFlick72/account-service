spring:
  application:
    name: account-service
  cloud:
    config:
      discovery:
        enabled: true
      fail-fast: true

logging:
  level:
    org:
      springframework:
        security: DEBUG